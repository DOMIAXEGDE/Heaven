validity_of_n_dimensional_data_a( x1, ..., xn ) {    f_a( x ) = x^a    f_a^( -1 )( x ) = x^( 1/a )    a ∈ N    a > 1    valid iff for every coordinate xi,    xi^( 1/a ) ∈ N}// validity_of_n_dimensional_data_a.cpp//// Enforces:////   f_a(x)     = x^a//   f_a^(-1)(x)= x^(1/a)//   a ∈ N, a > 1//// Valid iff for every coordinate xi, xi^(1/a) ∈ N//// This program treats "natural whole numbers" as non-negative integers://   N = {0, 1, 2, 3, ...}//// Usage://   ./validity <a> <x1> <x2> ... <xn>//// Exit codes://   0 = VALID//   1 = INVALID//   2 = USAGE / INPUT ERROR//// Example://   ./validity 2 1 4 9 16//   ./validity 3 1 8 27 64//   ./validity 3 1 8 9 27#include <charconv>#include <cstdint>#include <iostream>#include <limits>#include <optional>#include <string>#include <vector>using u64 = std::uint64_t;using u32 = std::uint32_t;enum class PowCompare {Less,Equal,Greater};static bool parse_u64(const std::string& s, u64& out) {if (s.empty()) return false;    const char* begin = s.data();    const char* end = s.data() + s.size();    auto result = std::from_chars(begin, end, out);    return result.ec == std::errc() && result.ptr == end;}static bool parse_u32(const std::string& s, u32& out) {if (s.empty()) return false;    u64 temp = 0;    if (!parse_u64(s, temp)) return false;    if (temp > std::numeric_limits<u32>::max()) return false;    out = static_cast<u32>(temp);    return true;}static PowCompare compare_power_to_target(u64 base, u32 exp, u64 target) {if (exp == 0) {return (1 == target) ? PowCompare::Equal: (1 < target ? PowCompare::Less : PowCompare::Greater);}    u64 value = 1;    for (u32 i = 0; i < exp; ++i) {        if (base == 0) {            value = 0;            break;        }        if (value > target / base) {            return PowCompare::Greater;        }        value *= base;    }    if (value == target) return PowCompare::Equal;    return (value < target) ? PowCompare::Less : PowCompare::Greater;}static std::optional<u64> exact_nth_root(u64 target, u32 a) {if (a <= 1) return std::nullopt;    if (target == 0) return 0;    if (target == 1) return 1;    if (a >= 64) {        return std::nullopt;    }    u64 low = 1;    u64 high = target;    while (low <= high) {        u64 mid = low + (high - low) / 2;        PowCompare cmp = compare_power_to_target(mid, a, target);        if (cmp == PowCompare::Equal) {            return mid;        } else if (cmp == PowCompare::Less) {            low = mid + 1;        } else {            if (mid == 0) break;            high = mid - 1;        }    }    return std::nullopt;}int main(int argc, char* argv[]) {if (argc < 3) {std::cerr<< "Usage: " << argv[0] << " <a> <x1> <x2> ... <xn>\n"<< "Example: " << argv[0] << " 2 1 4 9 16\n";return 2;}    u32 a = 0;    if (!parse_u32(argv[1], a) || a <= 1) {        std::cerr << "Error: a must be a natural whole number greater than 1.\n";        return 2;    }    std::vector<u64> coords;    coords.reserve(static_cast<std::size_t>(argc - 2));    for (int i = 2; i < argc; ++i) {        u64 x = 0;        if (!parse_u64(argv[i], x)) {            std::cerr << "Error: invalid natural whole number: " << argv[i] << "\n";            return 2;        }        coords.push_back(x);    }    bool all_valid = true;    std::cout << "Checking validity_of_n_dimensional_data_a for a = " << a << "\n";    std::cout << "Rule: valid iff for every coordinate xi, xi^(1/a) ∈ N\n\n";    for (std::size_t i = 0; i < coords.size(); ++i) {        u64 x = coords[i];        auto root = exact_nth_root(x, a);        if (root.has_value()) {            std::cout << "x" << (i + 1) << " = " << x                      << "  -> VALID"                      << "  (exact root = " << *root                      << ", because " << *root << "^" << a                      << " = " << x << ")\n";        } else {            std::cout << "x" << (i + 1) << " = " << x                      << "  -> INVALID"                      << "  (x^(1/" << a << ") is not a natural whole number)\n";            all_valid = false;        }    }    std::cout << "\nResult: " << (all_valid ? "VALID" : "INVALID") << "\n";    return all_valid ? 0 : 1;}Build it with:g++ -std=c++17 -O2 -Wall -Wextra -pedantic validity_of_n_dimensional_data_a.cpp -o validity.exeExample runs:./validity 2 1 4 9 16Result: VALID./validity 3 1 8 9 27x3 = 9  -> INVALIDResult: INVALID> This program enforces your rule exactly: each coordinate is valid only when it is a perfect (a)-th power.